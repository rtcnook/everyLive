// Root project intentionally keeps no shared build plugins.
// Each module declares only the plugins it needs so the Spring Boot server can
// build without resolving Android/Compose plugins from the legacy client app.
